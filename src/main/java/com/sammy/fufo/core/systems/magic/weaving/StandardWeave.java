package com.sammy.fufo.core.systems.magic.weaving;

/**
 * A standard weave, nothing to see here
 */
public class StandardWeave extends Weave<StandardWeave> {
    /**
     * Constructs a new weave, given a Bindable as a base.
     *
     * @param base
     */
    public StandardWeave(Bindable base) {
        super(base);
    }
}
